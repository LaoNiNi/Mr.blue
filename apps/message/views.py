from django.shortcuts import render
# 导入model
# from .models import UserMessage
#
# def getform(request):
#     all_message = UserMessage.objects.filter(name='大明',email='333@qq.com')
#     if all_message:
#         #all_message是个对象集合，可以通过索引取出其中的对象
#         message = all_message[0]
#         #render里可以传值给前端，其中前端取的是my_message这个变量名称
#     return render(request, 'index4.html',{"my_message":message}
#                   )
